"""Planning layer: `WorldState`/`Plan`/`Primitive` types, workspace geometry (fence walls,
placement search, reset checks), and the `Planner` protocol + concrete push/reset policies
(`RandomPushPlanner`, `SegPushPlanner`). See `autograsper/docs/planning.md`.
"""
